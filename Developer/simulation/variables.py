# Número de acciones posibles para el agente (en este caso, 3 acciones).
n_actions = 3

# Factor de decaimiento para epsilon (controla la tasa de disminución de la exploración).
epsilon_decay = 0.995

# Valor mínimo de epsilon (asegura que siempre haya una mínima exploración).
epsilon_min = 0.01

# Número de estados discretizados del entorno.
n_states = 8

# Valor inicial de epsilon (probabilidad de exploración al comienzo del entrenamiento).
epsilon = 1.0
